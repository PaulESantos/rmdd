% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdd-reconcile.R
\name{mdd_matching}
\alias{mdd_matching}
\title{Reconcile mammal names against MDD}
\usage{
mdd_matching(
  x,
  target_df = NULL,
  prefilter_genus = TRUE,
  allow_duplicates = FALSE,
  max_dist = 1,
  method = "osa"
)
}
\arguments{
\item{x}{Character vector of names or a data frame with parsed name columns.}

\item{target_df}{Optional backbone produced by \code{\link[=build_mdd_match_backbone]{build_mdd_match_backbone()}}.}

\item{prefilter_genus}{Logical. If \code{TRUE}, restrict the backbone to exact and
fuzzy candidate genera before the full pipeline.}

\item{allow_duplicates}{Logical. If \code{TRUE}, deduplicate internally and expand
results back to the original rows.}

\item{max_dist}{Maximum string distance used in fuzzy stages.}

\item{method}{Distance method passed to \verb{fuzzyjoin::stringdist_*_join()}.}
}
\value{
A tibble with row-level traceability, pathway flags, matched name
context, accepted-name context, and fuzzy distance columns.
}
\description{
Run a staged species-level reconciliation pipeline against an MDD backbone.
The workflow prioritizes exact evidence first, then progressively relaxes
criteria through exact genus, fuzzy genus, exact species-within-genus, and
fuzzy species-within-genus matching.
}
\examples{
mdd_matching(c("Puma concolor", "Felis concolor", "Pumma concolor"))
}
